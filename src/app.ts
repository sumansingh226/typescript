const myName: string = "suman singh";
console.log("myName", myName);

const num: number = 10;
const isValid: boolean = true;
let unDefinedValue: undefined;

enum enumExample {
    name = "suman",
    profession = "software Engineer",
    address = "Noida UP",
}

function myFun(): void {
    // Function body
}

const arrayExample: number[] = [1, 2, 3, 4, 5];
const objectExample: { name: string, age: number } = { name: "Alice", age: 30 };

console.log("arrayExample", arrayExample);
console.log("objectExample", objectExample);
